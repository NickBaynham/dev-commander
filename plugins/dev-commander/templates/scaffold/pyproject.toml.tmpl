[project]
name = "{{project_name}}"
version = "0.0.0"
description = "{{project_description}}"
requires-python = ">=3.12"
dependencies = []

[tool.pdm]
distribution = false

[dependency-groups]
dev = [
    "ruff>=0.6",
    "pytest>=8.0",
]
