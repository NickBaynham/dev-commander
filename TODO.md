# To Do

## v0.4 (planned — see planning/plan.md, Tasks 25-31)

- Phase 22: deployments/ workspace directory.
- Phase 23: Dockerfile template family (python, node-ts, go).
- Phase 24: dc-publish skill (build and push a container image to GHCR).
- Phase 25: production compose and release workflow templates.
- Phase 26: dc-deploy skill (deploy over SSH with docker compose).
- Phase 27: next_step ship state (/dc:publish and /dc:deploy before completion).
- Phase 28: v0.4 release.

## Later

- Registries beyond GHCR (Docker Hub, ECR, GAR) as a registry option.
- Deploy-target family beyond self-hosted SSH: container PaaS, Kubernetes.
- Language-native package publishing (PyPI, npm).
